#To use preloaded data in Rstudio
#To check the list of preloaded data
data()
#To check for mtcars data
data(mtcars)
#To use women data
data(women)
#To use volcano data
data("volcano")
#After running the script in the script pane
#It will appear in the environment pane, you click the data in the environmet pane
#Then the data will load in the console pane
#click the data name again in the environment pane
#it will appear in the script pane.
#To load Nile data
data(Nile)
data("Nile")
#or run view(dataname)